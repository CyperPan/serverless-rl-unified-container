# Pinned to match the cluster's working nitro_env (verified 2026-04-26).
# torch + torchvision are installed separately by Dockerfile/unified.def with
# the CUDA 12.1 wheel index — pip's default index doesn't carry +cu121 builds.
gymnasium>=1.0
gymnasium[atari,accept-rom-license]>=1.0
gymnasium[mujoco]>=1.0
ray[rllib]==2.8.0
mujoco>=3.0
pygame
redis
numpy<2
